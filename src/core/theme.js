import { DefaultTheme } from "react-native-paper";

export const theme = {
    ...DefaultTheme,
    colors: {
        ...DefaultTheme.colors,
        text: '#fff',
        primary: '#00A36C',
        secondary: '#413757',
        error: '#f13a59',
        background: 'white',
    }
}